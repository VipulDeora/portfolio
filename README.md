# Personal Website Phase 1

Local Vite + React + Tailwind project exported from the current canvas.

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL printed in your terminal, usually:

```bash
http://localhost:5173
```

## Main file

Edit:

```bash
src/App.jsx
```

That is where the homepage prototype lives.
